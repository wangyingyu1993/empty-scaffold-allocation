# Scripts

`analysis/` contains full analysis scripts for scaffold splitting, leakage metrics, metric sensitivity, and DataSAIL + ESA repair.

`models/` contains portable model-training examples for split CSV files.

The notebook workflows are the recommended entry points for the compact demo analyses.

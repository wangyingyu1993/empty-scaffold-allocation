# Tests

These tests cover the core package imports, scaffold detection, scaffold fold assignment, metric helpers, and the current demo-data layout used by the notebooks.

The old expected-output checks for FreeSolv, Tox21, and ESOL demo files have been removed.
